# Copy this file to the root-anchored .env file before running a real IB download.
# .env is intentionally ignored by Git: select paper/live endpoints outside source code.
IB_HOST=127.0.0.1
IB_PORT=7498
IB_CLIENT_ID=7
